default:
    suites:
        pickle:
            paths:    [ %paths.base%/features ]
            contexts: [ FeatureContext ]
        pickle_phar:
            paths:    [ %paths.base%/features ]
            contexts: [ PharFeatureContext ]
